dependencyResolutionManagement {
    repositories {
        google()
        jcenter()
    }
}
enableFeaturePreview('GRADLE_METADATA')

rootProject.name = "Tick"

include ':app'
include ':kgit'
include ':todo'
