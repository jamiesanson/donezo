package nz.sanson.tick.todo.feature.splash

import nz.sanson.tick.todo.Screen
import org.reduxkotlin.Reducer

/**
 * Not a lot happens on the splash screen.
 */
val SplashReducer: Reducer<Screen.Splash> = { state, _ -> state }
